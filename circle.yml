dependencies:
    override:
        - sudo pip install awscli
deployment:
  prod:
    branch: master
    commands:
        - aws s3 sync /home/ubuntu/S3ContinuousDeploy http://bakertst-s3continuous-deploy.s3.amazonaws.com --delete

